# Tools

Put `mpqcli.exe` in this directory if you want to rebuild the map locally:

```text
tools\mpqcli.exe
```

The binary is not committed to avoid bundling third-party/local executables in the repository.

